# Optional local settings. This file is documentation only; it is not auto-loaded.
# SENTINELAI_PORT=5000
# SENTINELAI_SECRET_KEY=replace-with-a-long-random-value

# Public deployments must set these values. Render sets them from render.yaml.
# SENTINELAI_PUBLIC_MODE=1
# SENTINELAI_PROXY_FIX=1
# SENTINELAI_TRUSTED_HOSTS=sentinel.example.com
# SENTINELAI_DB_PATH=/data/sentinelai.db
