# Optional environment variables. This file is documentation only: the app does not auto-load .env.
# Set with export in your terminal, or load using your own environment manager.
# Generate a secret locally; never commit a real secret.
# SENTINELAI_SECRET_KEY=replace-with-a-long-random-value
# SENTINELAI_DEBUG=1
# SENTINELAI_PORT=5000
